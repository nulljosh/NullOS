/* NullOS Shell
 *
 * Minimal command-line shell. Reads keyboard input,
 * parses commands, and executes built-in functions.
 *
 * See: docs/PHASES.md Phase 2.7
 */

/* TODO: Implement shell
 *
 * Built-in commands:
 *   help     - List available commands
 *   clear    - Clear screen
 *   echo     - Print text
 *   time     - Show uptime from PIT ticks
 *   reboot   - Triple-fault to reboot
 *   meminfo  - Show memory info (Phase 3)
 *   ls       - List files (Phase 5)
 *   cat      - Print file contents (Phase 5)
 *
 * Prompt: "null> "
 *
 * Functions:
 *   shell_run()                    - Main shell loop
 *   shell_execute(const char *cmd) - Parse and dispatch command
 */
